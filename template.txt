    "<name>": {
    "description": "<-- required",
    "website": "Optional",
    "html": [
      {
        "regex": "Regex for header value",
        "version": "regex for which capture group contains the version"
      }
    ],
    "link": [      {
        "regex": "Regex for header value",
        "version": "regex for which capture group contains the version"
      }]
    "url": [
      {
        "regex": "Regex for header value",
        "version": "regex for which capture group contains the version"
      }
    ],
    "js": {
      "<identifier>": "<optional regex to place the version number in an identifier group>"
    },
    "scriptSrc": [
      {
        "regex": "Regex for header value",
        "version": "regex for which capture group contains the version"
      }
    ],
    "scripts": [
      {
        "regex": "Regex for header value",
        "version": "regex for which capture group contains the version"
      }
    ],
    "headers": {
      "<Header key>": "Regex for header value." | {
        "regex": "Regex for header value",
        "version" : "regex for which capture group contains the version"
        }
    },
    "meta": {
      "attribute name": [
        {
          "regex": "Regex for header value",
          "version": "regex for which capture group contains the version"
        }
      ]
    }
  }
